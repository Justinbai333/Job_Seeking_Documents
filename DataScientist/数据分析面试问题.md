# 技术问题

1. **Q: R语言如何安装包？**
```
  A: install.packages('包的名字')，装完之后用library(包的名字load)
```
2. **Q：18-25岁这种连续性分段变量聚类时如何分组？**\
```
  A：两种方法：1.等距分段 2.最优分段（选择能最小化代价函数的分段方式）
```
3. **Q: K-Means的使用场景？**\
```
  A: 1.用户分类 2.单变量离散化
```
4. **2000万维数据如何降维** \
```
[数据降维](\数据降维方法总结.md)
```
5. **回归共线性怎么发现，如何处理** \
```
  A：通过VIF值发现，VIF大于5时说明有共线性。处理：
  1. 分析变量之间的correlation，手动剔除相关性高的变量。
  2. 增加样本容量
  3. 岭回归（正则化）
```
6. **sql窗口函数 rank(), dense_rank() row_number()的区别**

```sql
  在234名并列时:
  - rank() ：12225
  - dense_rank(): 12223
  - row_number():12345
```

7. **sql GROUP BY和partition by的区别**

```
group by会改变表格行数，让每一个group只有一行，partition by不会
```
8. **统计学 大数定理和中心极限定理**
```
见统计知识
```
9. **SQL语句的执行顺序**

```sql
1、from 子句组装来自不同数据源的数据；

2、where 子句基于指定的条件对记录行进行筛选；

3、group by 子句将数据划分为多个分组；

4、使用聚集函数进行计算；

5、使用 having 子句筛选分组；

6、计算所有的表达式；

7、select 的字段；

8、使用 order by 对结果集进行排序。


举例：

(1) FROM <left_table>

(3) <join_type> JOIN <right_table>

(2) ON <join_condition>

(4) WHERE <where_condition>

(5) GROUP BY <group_by_list>

(6) WITH {CUBE | ROLLUP}

(7) HAVING <having_condition>

(8) SELECT

(9) DISTINCT

(9) ORDER BY <order_by_list>

(10) <TOP_specification> <select_list>

以上每个步骤都会产生一个虚拟表，该虚拟表被用作下一个步骤的输入。
这些虚拟表对调用者(客户端应用程序或者外部查询)不可用。
只有最后一步生成的表才会会给调用者。
如果没有在查询中指定某一个子句，将跳过相应的步骤。

　　逻辑查询处理阶段简介：

　　1、 FROM：对FROM子句中的前两个表执行笛卡尔积(交叉联接)，生成虚拟表VT1。

　　2、 ON：对VT1应用ON筛选器，只有那些使为真才被插入到TV2。

　　3、 OUTER JOIN:如果指定了OUTER JOIN(相对于 CROSS JOIN 或 INNER JOIN)，
      保留表中未找到匹配的行将作为外部行添加到VT2，生成TV3。
      如果FROM子句包含两个以上的表，则对上一个联接生成的结果表和下一个表重复执行步骤1到步骤3，直到处理完所有的表位置。

　　4、 WHERE：对TV3应用WHERE筛选器，只有使为true的行才插入TV4。

　　5、 GROUP BY：按GROUP BY子句中的列列表对TV4中的行进行分组，生成TV5。

　　6、 CUTE|ROLLUP：把超组插入VT5，生成VT6。

　　7、 HAVING：对VT6应用HAVING筛选器，只有使为true的组插入到VT7。

　　8、 SELECT：处理SELECT列表，产生VT8。

　　9、 DISTINCT：将重复的行从VT8中删除，产品VT9。

　　10、ORDER BY：将VT9中的行按ORDER BY子句中的列列表顺序，生成一个游标(VC10)。

   11、TOP：从VC10的开始处选择指定数量或比例的行，生成表TV11，并返回给调用者。
```

10. **数据库内连接和外连接的区别**

```sql
Inner Join, Outer Join, Left Join, Right Join:
中文分别叫内连接 外连接 左外连接 右外连接

INNER JOIN：两表链接的项都要符合链接条件
Outer JOIN: 都不用
LEFT JOIN: 左边不用
RIGHT JOIN: 右边不用
```


11. **线性回归中随机误差的假设**

```
y = b0 + b1x + μ

μ符合以下四个假设：

1. 假设μ服从正态分布
2. 假设μ的期望值是0
3. 假设μ的同方差特性，即Var(μ)为一个常数
4. 假设μ和自变量x的关联性Cov(μ, x) = 0
```

12. **SQL中UNION 和UNION ALL的区别**

```sql
UNION 会对合并后的数据进行 去重 和 排序
UNION ALL 不会，所以 UNION ALL 的效率更高
```


# 业务问题

1. **如何分析销售数据，从什么维度看**
```
  销售数据中的指标种类：

  - 销售效率指标
  - 市场占有率指标
  - 盈利能力指标
  - 营运能力指标
  - 渠道客户指标
  - 辅助分析指标
  - 销售过程指标

  不同维度：

  1. 整体销售分析:

    销售额 / 销售量： 分析近几年的销售额，销售量，并于行业标准，对手相比较。
    季节因素：分析销售的淡旺季，产品的需求是否具有周期性。
    产品线：通过产品结构分析，了解重点产品表现
    价格体系：通过价格体系分析，了解企业优势价格区间

  2. 区域布局分析

    区域分布：分析企业各区域表现，发掘重点区域、潜在市场
    区域销售异动分析：对增长和下跌明显的区域重点分析

  3. 产品线分析

    产品线结构分析：分析产品系列和单产品的结构分布，检索重点产品和新产品的市场表现
    产品-区域分析：多要素复合型分析，不同区域各产品线的表现分析

  4. 价格体系分析

    价格体系分析：划分出符合实际的价格区间划分标准，寻找主导价位。
    价格-产品分析
    价格-区域分析
```

[参考链接](\http://www.woshipm.com/data-analysis/1806245.html)


3. **如何评估功能上线的效果**

4. **如何评估模型效果**

5. **留存率公式**

```
留存率 = 新增用户中登录用户数 / 新增用户数 * 100%

新增用户数：在某个时间段（一般为第一整天）新登录应用的用户数
登录用户数：新登录应用后至当前时间，至少登录过一次的用户数

举例：

7日用户留存 = 新增用户中，第7天还继续登录应用的用户数量 / 第一天新增的用户数量

```


6. **对移动互联网的认识**

7. **如何针对一个业务选取指标**

8. **如何增加xx软件用户的停留时长**

9. **如何提升xx指标**

10. **数据分析、数据挖掘和数据科学有什么区别？**

11. **t-test的业务应用**

12. **AB TEST如果不显著怎么办？**

13. **怎么体现同质性？** \
描述性统计，聚集度，离心度

14. **留存率下降如何分析**

```
整体框架见Q18

四个步骤：

1. 判断数据真实性

2. 拆分维度分析：

    那个部分的用户留存率变低比较多？是安卓用户还是IOS用户？主要集中在哪个城市？
    对指标异动做一个初步的识别

3. 指标拆分分析：

    留存率 = 注册x日仍在登录的用户 / 第x天前注册的新用户

    是新用户多了还是x日仍在登录的用户少了？

4. 原因分析：

    - 内部原因：是否有版本更新导致的使用bug？
               是否某个运营活动结束，新用户主要是为了活动注册的？
    - 外部原因：出现新的竞品？
               舆论对于公司 / 产品是否有负面报道？
               是否竞品开发了新的功能？
```
15. **DAU下降如何分析**

```

```
16. **人力素质模型**

17. **如何给某一个APP简历数据指标体系**

18. **指标拆解 异动分析**

某指标（如今日登录用户）突然下降，如何分析？

```
1. 确认数据真实性，是否可能是统计数据出了问题

2. 拆分维度分析：

  - 新用户/ 老用户
  - 不同登录平台，安卓 / IOS
  - 不同版本：新版本 / 老版本
  - 登陆渠道：小程序，app，网页
  - 不同地区：省份 / 国家
  - 时间：淡旺季 / 日夜 / 产品周期性

3. 指标拆解：

  - 销售额 = 入站流量*下单率*客单价
  - 入站流量 = Σ各来源流量*转化率
  - 下单率 = 页面访问量*转化率
  - 客单价 = 商品数量*商品价格

  拆解之后分析具体是哪个指标出现异动

4. 原因分析：

  - 内部原因：

      产品原因：版本迭代 / 功能改动
      运营原因：运营活动 / 广告投放

  - 外部原因：

      PEST分析：
        p政治
        e经济（长期看整个行业，短期看竞争环境）
        s社会（舆论）
        t技术（新技术的出现）

      产品调研方法论：
        市场
        竞品
        用户
```


19. **平台收入下降怎么分析**

20. **如何量化分析一个企业的效率**

21. **如何知道一个人是否属于高收入群体**

22. **如何知道一个人的住址**

23. **辛普森悖论：现象和解决方式**


# 统计 / 数学 / 概率论问题

1. **圆上任意挑选一点，到圆心的距离**

```
在圆内任意划出一个半径为 x < R的同心圆，点落在小圆的概率为 (x² / R²)

求导可以得出落在边上的概率为 2x / R²

到圆心的距离的期望就是 x * (2x / R²) dx 从0积分到R = 2R/3

```
2. **w检验：Shapiro - Wilk Test**

```
用来检验样本是否符合正态分布，W值越接近1，分布越拟合正态分布
```

3. **极大似然估计的原理及应用**

```
极大似然估计实在模型已知的情况下根据样本分布拟合模型的参数，即“模型已定，参数未知”

极大似然估计需要满足的条件：所有的采样都是独立同分布的(i.i.d.)

具体应用场景：

样本太多，无法得出分布的参数值，可以采样小样本后，利用极大似然估计获取假设中分布的参数值。

举例：人口普查，市场调查，用户调研
```
